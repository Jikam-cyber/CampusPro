services:
  - type: web
    name: campuspro
    env: java
    buildCommand: mvn clean package -DskipTests
    startCommand: java -jar target/campuspro.jar
    envVars:
      - key: PORT
        value: 8080
